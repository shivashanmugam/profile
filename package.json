{
  "name": "sample-server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^10.0.0",
    "ejs": "^2.6.1",
    "express": "^4.17.0",
    "gatsby-theme-blog-darkmode": "^3.0.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "client-build":"cd client && yarn build",
    "blog-build":"cd client && cd blog && yarn build",
    "build-both":"yarn blog-build && yarn client-build"
  },
  "author": "",
  "license": "ISC"
}